<div class="p10">
<?php
	/*$menuVista = MenuVista::getInstance();
	$menus = $menuVista->getMenus();
	if( !is_null( $menus ) ) {
		$tipoGadget = ORM::factory( "Tipomenu" )->gadget;
		foreach( $menus as $menu ) {
			if( $menu->visible == 1 && $menu->idtipomenu == $tipoGadget ) {
				$parametros = "/index/controller=" . $menu->url;
				$parametros .= "&link=" . $menu->urlimagen;
				Request::factory( $menu->url . $parametros )->execute();
			}
		}
	}*/
?>
</div>